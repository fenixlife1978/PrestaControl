import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function ReportsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Reportes</CardTitle>
        <CardDescription>
          Aquí puede ver y generar reportes financieros.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>El contenido del módulo de reportes estará disponible aquí.</p>
      </CardContent>
    </Card>
  );
}
