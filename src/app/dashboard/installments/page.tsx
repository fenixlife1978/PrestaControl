import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function InstallmentsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Cuotas</CardTitle>
        <CardDescription>
          Aquí puede gestionar y revisar las cuotas de los préstamos.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>El contenido del módulo de cuotas estará disponible aquí.</p>
      </CardContent>
    </Card>
  );
}
